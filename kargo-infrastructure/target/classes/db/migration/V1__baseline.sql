CREATE TABLE customers (
        id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        full_name VARCHAR(200) NOT NULL,
        email VARCHAR(200) NOT NULL CONSTRAINT uk_email UNIQUE,
        phone VARCHAR(40),
        created_at TIMESTAMP
        );